# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
cache: packages

os: 
  - osx
  - linux

branches:
  only:
    - master
    - develop
    
use_bioc: true
bioc_required: true
bio_packages:
  - phyloseq